<parameter name="content">package com.aiu.scrs.entity;

/**
 * Complaint Status Enum - Tracks the lifecycle of complaints
 */
public enum ComplaintStatus {
    PENDING,      // Initial state when complaint is created
    IN_PROGRESS,  // Staff member is working on the complaint
    RESOLVED,     // Complaint has been resolved
    CLOSED,       // Complaint is closed and final
    REJECTED      // Complaint was rejected
}
</parameter>
